# Development dependencies for MANSA backend.
# Install with: pip install -r server/requirements-dev.txt

# ── Test runners ──
pytest>=8.0
pytest-cov>=5.0
pytest-flask>=1.3

# ── Lint & format ──
ruff>=0.6
mypy>=1.10

# ── Security audit ──
bandit>=1.7
pip-audit>=2.7

# ── Type stubs ──
types-requests
types-beautifulsoup4
